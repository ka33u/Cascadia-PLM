package-lock.json
pnpm-lock.yaml
yarn.lock

# Generated build output
.output
.nitro
dist
dist-ssr
html/

# Generated by TanStack Router during `vite build` — not hand-edited
packages/core/src/routeTree.gen.ts
apps/*/src/routeTree.gen.ts
packages/core/src/lib/api/openapi-types.gen.ts

# Generated by `npm run openapi:snapshot`, which writes them with
# JSON.stringify(spec, null, 2). Prettier collapses short arrays onto one
# line, so formatting these files makes `npm run openapi:check` fail.
#
# Two of them: the API contract is edition-specific, and the second is the
# community spec the overlay publishes in place of this tree's.
docs/api/openapi.v1.json

# Claude Code worktrees under the primary checkout. Git hides them via
# .git/info/exclude, which Prettier does not read — so a format:check run in
# the primary checkout otherwise scans every worktree's full tree, where the
# anchored ignores above (e.g. docs/api/openapi.v1.json) do not match the
# nested copies and even a pristine worktree fails the scan. It also keeps
# `npm run format` from rewriting files inside live worktrees. CI checkouts
# never contain these.
.claude/worktrees/

# Generated by `npm run db:generate` (drizzle-kit), which writes its own
# JSON layout. Formatting them means every future generate re-dirties the
# tree; the files are machine-read only.
apps/*/drizzle/meta/

# Written by pytest when the Python worker suite runs (see
# docs/development/testing.md). It carries a README.md that Prettier would
# otherwise reformat, so running the tests before `npm run format:check`
# failed the check on a file nothing here wrote. Git already ignores the
# directory via the .gitignore pytest drops inside it; Prettier does not
# read that one.
.pytest_cache/
