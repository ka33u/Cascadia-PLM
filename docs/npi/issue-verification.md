# 原生项目问题 验证结果

临时副本：/tmp/cascadia-issue-review。该阶段正式源码尚未改动；获得通过证据后已批准写入并成功执行0008迁移。

实际执行结果：
- HTTP测试16/16通过，含原有BOM/承诺回归、原生Issue事务回滚、采购和无关技术账号隔离、主管只读、跨项目关联拒绝、禁用账号拒绝、修改原因和过期版次拒绝、生命周期完整处理历史、重大风险解除。
- 正式构建成功；core/app/root类型检查全部通过；新Issue模块ESLint零错误零警告。
- 真实浏览器使用测试库会话：技术建重大问题、采购手机补充处理记录、处理中→已解决→已验证→关闭；采购界面无修改责任人/严重度入口；重大风险在关闭后解除。
- 桌面和390px手机截图已检查，保存和状态操作按钮可辨识。

权限原则：原有getActor/loadProject/owner鉴权体不改变，仅导出供Issue服务复用；采购只能访问/处理本人问题，不能进入完整项目。所有新端点继承原有会话、同源和X-NPI-Actor校验。主管无写权限；未新增角色或Program成员。

原生ItemService唯一改变：create增加可选tx参数、以withTx复用调用者事务；实际多连接数据库测试已证明NPI扩展校验失败时原生Issue不会遗留。状态推进完全调用原生LifecycleInstanceService，不设置绕过参数。

写入前核验的文件和哈希见npi-issue-reviewed-manifest.json；只包含13个指定文件。新增迁移0008只有npi_issue_links扩展表、外键和索引。
