# 本地运行状态核查

2026-09-15对当前独立系统执行只读核查，未登录、重启服务或写入业务数据。

- 3410 页面和3411接口均存在监听进程，工作目录都是 `/Users/zhangyu/Documents/Cascadia-NPI/app`。
- 3410运行Vite，`/npi`返回200，包含Vite开发客户端。
- 3411运行Cascadia开发入口，其父进程使用tsx watch自动重载。
- 3411匿名NPI元数据请求返回401；通过3410代理同样返回401。
- 从3410实际读取的项目页模块包含NpiBottleneck、predictionNote和onLocate；预测提示模块包含BOM_REVIEW_PENDING、PENDING_REPLY及“预测不完整”。
- 核对18个NPI后端及入口文件，最新修改与API进程启动落在同一秒，没有发现晚于启动秒的源码修改。进程启动时间只有秒级精度，不能据此证明内存模块逐字节相同。

结论：当前运行服务来自独立系统目录，前端正在返回近期已交付功能的代码；目前没有需要重启服务的迹象。页面代码标记及匿名响应检查不替代登录后的业务回归，设置页待审候选也没有据此同步。

原始核查结果见 [运行记录](local-runtime-verification.json)。记录为核查时快照，端口和进程状态可能在后续启动、关闭或重载后改变。
