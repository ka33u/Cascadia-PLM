# 独立版本回归记录

日期：2026-09-16。验证对象为本仓库独立应用，不沿用旧应用的通过记录。

| 范围 | 验证内容 |
| --- | --- |
| 安装与构建 | 标准 `npm ci`，TypeScript 检查，Vite 前端和独立服务端构建；npm 依赖审计 0 个已知漏洞 |
| 规则与解析 | 33 项：BOM 层级、精确小数、版本匹配、齐套口径、跟踪建议、母件确认、文件识别以及构建别名唯一性 |
| HTTP 业务 | 37 项：五岗位权限、草稿、导入、原件下载、承诺历史、并发更新、制造计划、完成记录、整体交接、继承与问题状态 |
| 账号与附件 | 13 项：首次改密、重置、停用、改岗并发、会话失效、登录锁定、上传幂等、权限、损坏校验和归档留存 |
| 数据迁移 | 2 项：坏文件触发完整回滚；账号与 UUID、精确 BOM 数据、首次承诺、问题历史和附件字节保持一致，重复迁移拒绝覆盖 |
| 真实账号界面 | 桌面登录和新增账号、手机首次改密与确认校验、管理员重置导致退出、主动退出登录 |
| 完整业务界面 | 桌面 1440px / 手机 390px；项目、BOM、采购、制造、问题、附件、交接、继承、模板、账号改岗和只读控制 |
| 大 BOM | 实际导入 5,000 行 / 8 层，分页、搜索、展开收起、末行跟踪及手机布局 |
| 真实 ERP 样本 | 两份样本分别为 47 行 / 16 个根节点、56 行 / 23 个根节点，均为 4 层；原始 Excel 下载逐字节一致 |
| 首屏与 Issue #1 | 桌面 / 手机登录页及已登录账号页；相对路径与别名共用一个上下文，构建产物中 guard 仅一份，无原 Toast / Sidebar 上下文 |

所有诊断项目、测试账号及回归写入均使用独立 `_test` 数据库。正式迁移单独执行，实际旧正式库包含 1 个账号、0 个新品项目；原库只读保留，原会话不迁移。

GitHub Actions 工作流 `Independent NPI checks` 在 Windows 和 Linux 分别执行干净安装、类型检查、单元测试、构建与真实浏览器首屏；Linux 另运行 PostgreSQL、迁移和真实账号界面回归。远端运行结果以 GitHub Actions 对应提交为准。仓库不包含私有 ERP 原始文件、密码、运行数据库或附件目录。

容器配置已提供；本机没有 Docker 引擎，本轮运行验证使用本机 Node.js 与 PostgreSQL，不将容器配置视为已经过容器运行验收。
