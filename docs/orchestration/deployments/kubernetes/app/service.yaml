apiVersion: v1
kind: Service
metadata:
  name: cascadia-app
  namespace: cascadia
  labels:
    app: cascadia-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: cascadia-app
