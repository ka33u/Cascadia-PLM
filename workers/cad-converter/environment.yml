name: cad-converter
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pythonocc-core>=7.7
  - numpy>=1.24
  - pip
  - pip:
      - pika>=1.3
      - 'psycopg[binary]>=3.1'
      - pydantic>=2.0
      - pydantic-settings>=2.0
      - pytest>=7.0
