# 代码与许可证

本版本为独立的「新品协同」应用。当前源码、构建、数据库、登录、问题和附件服务不依赖 Cascadia 应用框架。

沿用此前在本仓库开发的 NPI 业务规则、界面及相关测试，保留其 AGPL-3.0-or-later 许可标记与仓库许可证。旧版本与原有版权信息保留在 Git 历史中。

React、Hono、Vite、Drizzle、ExcelJS、Radix UI 等通用开源库通过 npm 安装，各自许可证随依赖发布。它们不构成旧 PLM 应用运行依赖。
